# 本地预览

`127.0.0.1:4173` 是本机临时地址，需要 Node 服务运行时才能访问。电脑重启后服务会停止，这是正常现象。

每次重启后，双击项目目录里的 `start-site.bat` 即可：

1. 启动本地静态服务器。
2. 自动打开 `http://127.0.0.1:4173/`。
3. 预览期间不要关闭启动窗口。

如果提示端口被占用，可以在 PowerShell 中运行：

```powershell
$env:PORT=5173
node server.js
```

然后访问 `http://127.0.0.1:5173/`。
